# Data format

Provide a CSV or JSONL containing:
- `image_path`: path to image file (absolute or relative to `data_root` in config)
- `text`: text string

Example CSV:
```
image_path,text
images/0001.jpg,"a cat on a sofa"
images/0002.jpg,"two dogs in the park"
```
